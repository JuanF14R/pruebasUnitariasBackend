//Siempre primero las importaciones

import { app } from "../../app.js";



const port = process.env.PORT 

// Ejecutar el servidor en nuestro computador
app.listen(port, () => {
    console.log("El servidor se esta ejecutando coreectamente, en el puerto", port);
});      
